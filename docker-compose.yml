version: "3.9"

services:
  api:
    build: .
    command: uvicorn global_hazard_intel.main:app --host 0.0.0.0 --port 8000
    env_file:
      - .env.example
    ports:
      - "8000:8000"
    volumes:
      - .:/app
